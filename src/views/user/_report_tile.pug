mixin report_tile(report)
  div(class="card" style="width: 18rem;")
    div(class="card-body") 
      h5(class="card-title")=report.name
      h6(class="card-subtitle mb-2 text-muted")=report.address
      p(clas="card-text")=report.description
